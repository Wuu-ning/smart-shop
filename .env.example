# 数据库配置（二选一）

# 方式一：使用 MySQL（推荐）
# 格式: mysql+pymysql://用户名:密码@主机:端口/数据库名?charset=utf8mb4
# 请根据你的 MySQL 配置修改
DATABASE_URL=mysql+pymysql://root:123456@localhost:3306/shop?charset=utf8mb4

# 方式二：使用 SQLite（默认，无需任何配置）
# 注释掉上面的 DATABASE_URL 即可
